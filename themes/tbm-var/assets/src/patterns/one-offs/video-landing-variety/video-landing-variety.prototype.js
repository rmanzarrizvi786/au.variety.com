const header = require( '../../modules/header/header.prototype' );

const main_menu = require( '../../modules/main-menu/main-menu.prototype' );

const video_header = require( '../../modules/video-header/video-header.prototype' );

const popular_videos = require( '../../modules/popular-videos/popular-videos.prototype' );

const explore_playlists = require( '../../modules/explore-playlists/explore-playlists.prototype' );

const footer = require( '../../modules/footer/footer.prototype' );

module.exports = {
	header,
	main_menu,
	video_header,
	popular_videos,
	explore_playlists,
	footer,
};
