const header = require( '../../modules/header/header.prototype' );

const main_menu = require( '../../modules/main-menu/main-menu.prototype' );

const video_header = require( '../../modules/video-header/video-header.playlist' );

const video_grid = require( '../../modules/video-grid/video-grid.prototype' );

const explore_playlists = require( '../../modules/explore-playlists/explore-playlists.full' );

const footer = require( '../../modules/footer/footer.prototype' );

module.exports = {
	header,
	main_menu,
	video_header,
	video_grid,
	explore_playlists,
	footer,
};
