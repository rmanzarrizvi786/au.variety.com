import initSelectNav from '@larva-js/interface/SelectNav';
import initProfileFilter from '@larva-js/interface/ProfileFilter';

window.addEventListener( 'load', () => {
	initSelectNav();
	initProfileFilter();
} );
