/**
 * WordPress dependencies.
 */
import { InnerBlocks } from '@wordpress/block-editor';

/**
 * Save inner block content.
 *
 * @return {JSX.Element} Polar block's markup.
 */

const Save = () => {
	return <InnerBlocks.Content />;
};

export default Save;
