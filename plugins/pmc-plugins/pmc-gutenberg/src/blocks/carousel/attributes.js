/**
 * Attributes store state for the block that can be changed by the user.
 */
const attributes = {
	curationTaxonomy: {
		type: 'string',
		default: null,
	},
	termId: {
		type: 'string',
		default: null,
	},
};

export { attributes };
